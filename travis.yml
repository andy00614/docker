language: node_js
node_js:
  - "12"
services:
  - docker

script:
  - echo  "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker build -t andy00614/docker-nginx:latest .
  - docker push pea3nut/docker-nginx:latest
